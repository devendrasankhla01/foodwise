export type Role = 'ADMIN' | 'INSTITUTION' | 'RECIPIENT' | 'LOGISTICS';
export interface User { id:string; email:string; passwordHash:string; name:string; role:Role; organizationId:string; active:boolean }
export interface Organization {id:string; name:string; type:Role; subtype:string; address:string; verified:boolean; active:boolean; available:boolean; afterHours:boolean; capacity:number; requirement:number; dietary:string[]; categories:string[]; pickup:boolean; refrigeration:boolean; distance:number; openHour:number; closeHour:number; buffer:number; vehicle:string; phone:string}
export interface Row {id:string; organizationId:string; createdAt:string; [key:string]:unknown}
export interface Pos extends Row {transactionId:string; date:string; itemName:string; quantitySold:number; mealType:string; unitPrice:number}
export interface Surplus extends Row {foodName:string; category:string; dietaryType:string; quantity:number; unit:string; servings:number; preparedAt:string; detectedAt:string; availableUntil:string; storageMethod:string; temperature:number|null; packagingStatus:string; declaration:boolean; contaminated:boolean; organic:boolean; status:string; assessment:string; assessmentReasons:string[]; afterHours:boolean; recipientId?:string; partnerId?:string; pickupMode?:string; pickupCode?:string; deliveryCode?:string; receivedQuantity?:number; timeline:{at:string;action:string;actor:string}[]; cv?:Record<string,unknown>; estimatedTravelTime?:number; matchReasons?:string[]; rejectedRecipientIds?:string[]; offeredAt?:string; codeAttempts?:number; reviewRequired?:boolean}
export interface State {users:User[]; organizations:Organization[]; inventory:Row[]; events:Row[]; production:Row[]; pos:Pos[]; imports:Row[]; forecasts:Row[]; surplus:Surplus[]; notifications:Row[]; audit:Row[]; recovery:Row[]; reports:Row[]; settings:{kgPerServing:number;co2PerKg:number;costPerKg:number;minimumWindowMinutes:number;pickupDelayMinutes:number}}
export interface Actor {id:string;role:Role;organizationId:string;name:string}
